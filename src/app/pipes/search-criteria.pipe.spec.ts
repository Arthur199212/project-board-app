import { SearchCriteriaPipe } from './search-criteria.pipe';

describe('SearchCriteriaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCriteriaPipe();
    expect(pipe).toBeTruthy();
  });
});
